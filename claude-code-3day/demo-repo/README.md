# status-service — Claude Code demo repo

A deliberately small Node service with no dependencies, used for the Block 1 live demo.

- `npm test` runs three passing tests using Node's built-in test runner.
- No `npm install` required — nothing to download, nothing to compile.

Demo prompt: *"Add a /health endpoint that returns the service version from package.json, and run the tests."*
